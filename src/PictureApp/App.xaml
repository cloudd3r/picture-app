<Application x:Class="PictureApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnLastWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Цвета акцента — светлый акрил -->
            <SolidColorBrush x:Key="WindowChromeBrush" Color="#26FFFFFF" />
            <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="#33FFFFFF" />
            <SolidColorBrush x:Key="ButtonBackgroundHoverBrush" Color="#66FFFFFF" />
            <SolidColorBrush x:Key="ButtonBackgroundPressedBrush" Color="#99FFFFFF" />
            <SolidColorBrush x:Key="ButtonForegroundBrush" Color="#FF1F1F1F" />
            <SolidColorBrush x:Key="ButtonStrokeBrush" Color="#552B2B2B" />

            <!-- Стиль навигационной кнопки (круглая со стрелкой) -->
            <Style x:Key="NavButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="48" />
                <Setter Property="Height" Value="48" />
                <Setter Property="Foreground" Value="{StaticResource ButtonForegroundBrush}" />
                <Setter Property="FontSize" Value="22" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Focusable" Value="False" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Ellipse x:Name="Bg"
                                         Fill="{StaticResource ButtonBackgroundBrush}"
                                         Stroke="{StaticResource ButtonStrokeBrush}"
                                         StrokeThickness="1.2" />
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  TextElement.FontSize="{TemplateBinding FontSize}"
                                                  TextElement.FontWeight="{TemplateBinding FontWeight}" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Fill"
                                            Value="{StaticResource ButtonBackgroundHoverBrush}" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bg" Property="Fill"
                                            Value="{StaticResource ButtonBackgroundPressedBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
